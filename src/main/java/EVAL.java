public class EVAL {
    //dynamic class
    // EVAL(vector<int>) -> float
    // [0, 3, 2, 1]
    // every city is connected
    // we start from the default, we go to every city and calc the distance, we dont have to calc it every time
    // we can make matrix of distances in

}
